/**
 * Data Provider 标准类型 - 与 AGENTS.md、README 一致。
 * 从 src/types 聚合，供 provider 与 adapter 使用。
 */

export type { PageRequest, PageResponse } from '@/types/pagination'
export type { NormalizedApiError, ApiErrorKind } from '@/types/errors'
